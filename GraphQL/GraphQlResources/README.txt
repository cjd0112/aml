This is a slightly modified copy of GraphiQL package. 
Needed for debugging some problems with GraphiQL setup - but now not (necessarily) needed
although it gives a good example of linking to embedded HTML resources in a .DLL. 